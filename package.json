{
  "name": "dealpilot-ai",
  "version": "1.0.0",
  "private": true,
  "description": "AI car-buying assistant — a buyer's agent, not a listings site",
  "type": "module",
  "scripts": {
    "dev": "netlify dev",
    "deploy": "netlify deploy --prod"
  },
  "engines": { "node": ">=20" },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
